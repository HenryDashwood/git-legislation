{
  "name": "git-legislation-web-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788 -c wrangler.jsonc -c ../read-api/wrangler.jsonc",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20250620.0",
    "@types/markdown-it": "^14.1.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.20.0"
  }
}
